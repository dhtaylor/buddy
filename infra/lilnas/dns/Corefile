. {
    errors
    log
    # Resolve the Buddy hostname locally; everything else falls through to forward.
    hosts {
        192.168.1.197 buddy.lan
        fallthrough
    }
    forward . 1.1.1.1 1.0.0.1
    cache 30
    reload
}
