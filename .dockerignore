# Don't copy host (Windows) build artifacts into the Linux image — the container
# installs its own node_modules and builds its own dist.
**/node_modules
**/dist
**/*.tsbuildinfo
web/dev-dist
data
backups
.git
.github
.githooks
*.log
.commitmsg.tmp
*.sqlite
*.sqlite-*
