{
  "name": "buddy",
  "private": true,
  "version": "0.1.0",
  "description": "Buddy — self-hosted family finance & budget app",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "dev": "npm run build --workspace shared && concurrently -n server,web -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace web",
    "test": "vitest run",
    "seed": "npm run seed --workspace server",
    "db:migrate": "npm run db:migrate --workspace server",
    "backup": "npm run backup --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
