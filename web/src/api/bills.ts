/**
 * STUB — owned by the bills feature agent.
 * Add TanStack Query hooks here that call `api.*('/bills/...')`.
 * The `/api/bills` server route plugin and the <Bills> page are already wired.
 */
import { api } from './client.js';

// Example pattern (uncomment + adapt):
// import { useQuery } from '@tanstack/react-query';
// export function useBills() {
//   return useQuery({ queryKey: ['bills'], queryFn: () => api.get('/bills') });
// }

export {};
void api;
