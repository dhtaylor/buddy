/**
 * STUB — owned by the budget feature agent.
 * Add TanStack Query hooks here that call `api.*('/budget/...')`.
 * The `/api/budget` server route plugin and the <Budget> page are already wired.
 */
import { api } from './client.js';

// Example pattern (uncomment + adapt):
// import { useQuery } from '@tanstack/react-query';
// export function useBudget() {
//   return useQuery({ queryKey: ['budget'], queryFn: () => api.get('/budget') });
// }

export {};
void api;
