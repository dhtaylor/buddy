/**
 * STUB — owned by the imports feature agent.
 * Add TanStack Query hooks here that call `api.*('/imports/...')`.
 * The `/api/imports` server route plugin and the <Imports> page are already wired.
 */
import { api } from './client.js';

// Example pattern (uncomment + adapt):
// import { useQuery } from '@tanstack/react-query';
// export function useImports() {
//   return useQuery({ queryKey: ['imports'], queryFn: () => api.get('/imports') });
// }

export {};
void api;
