/**
 * STUB — owned by the history feature agent.
 * Add TanStack Query hooks here that call `api.*('/history/...')`.
 * The `/api/history` server route plugin and the <History> page are already wired.
 */
import { api } from './client.js';

// Example pattern (uncomment + adapt):
// import { useQuery } from '@tanstack/react-query';
// export function useHistory() {
//   return useQuery({ queryKey: ['history'], queryFn: () => api.get('/history') });
// }

export {};
void api;
