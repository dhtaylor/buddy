/**
 * STUB — owned by the ledger feature agent.
 * Add TanStack Query hooks here that call `api.*('/ledger/...')`.
 * The `/api/ledger` server route plugin and the <Ledger> page are already wired.
 */
import { api } from './client.js';

// Example pattern (uncomment + adapt):
// import { useQuery } from '@tanstack/react-query';
// export function useLedger() {
//   return useQuery({ queryKey: ['ledger'], queryFn: () => api.get('/ledger') });
// }

export {};
void api;
